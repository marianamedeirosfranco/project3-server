/* const router = require('express').Router();

//import the models
const User = require('../models/User.model');
const { isAuthenticated } = require("../middleware/jwt.middleware.js");

//TO BE CONFIRMED IF WE NEED */